import { createTheme } from "@mui/system";
const theme = createTheme({
  palette: {
    primary: {
      main: "#eab",
      light: "blue",
    },
    secondary: {
      main: "#ecd",
      light: "gold",
    },
    otherColor: {
      main: "#000",
      light: "magenta",
    },
  },
});
export default theme;
